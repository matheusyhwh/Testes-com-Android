<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ufcg.es.es_front">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.AboutActivity" />
        <activity android:name=".Activities.CreateVehicleActivity" />
        <activity android:name=".Activities.RegisterUserActivity" />
        <activity android:name=".Activities.SettingsActivity" />
        <activity android:name=".Activities.CreateServiceActivity" />
        <activity
            android:name=".Activities.CarListActivity"
            android:label="@string/title_activity_car_list" />
        <activity android:name=".Activities.VehicleActivity" />
        <activity
            android:name=".Activities.SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.TestezinhoActivity"
            android:label="@string/title_activity_testezinho"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>